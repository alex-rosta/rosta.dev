/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.postimg.cc',
      'assets.aceternity.com',
      'images.unsplash.com',
      'i.scdn.co',
    ],
  },
};

export default nextConfig;
